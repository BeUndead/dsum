plugins {
    id 'java'
}

group = 'com.com'
version = '0.0.1-SNAPSHOT'
description = 'dsum'

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

repositories {
    mavenCentral()
}

dependencies {
}

tasks.named('test') {
    useJUnitPlatform()
}
